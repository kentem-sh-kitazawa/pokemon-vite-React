import "../styles/PageButton.css"

const PageButton = () => {
  return (
    <div className="page-button">
      <button>前へ</button>
      <button>次へ</button>
    </div>
  );
};
export default PageButton;
